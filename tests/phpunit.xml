<phpunit bootstrap="./TestHelper.php">
    <testsuite name="Processes - Testsuite">
        <directory>./../application/</directory>
    </testsuite>

    <!-- Enable this for proper unit testing code coverage reports
    <filter>
        <whitelist>
            <directory suffix=".php">../library/Zend</directory>
        </whitelist>
    </filter>
    -->
</phpunit>
